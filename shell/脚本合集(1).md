=============================================================

需求说明 ：现企业需要每日巡检服务器查看服务器资源使用情况 ，一旦超过资源的80%就进行告警

监控对象

CPU使用率、内存使用率、磁盘使用率（企业可能使用存储挂载）、磁盘IO利用率、网卡流量（收/发）

以上设置告警阀值（在企业中根据企业要求调整）

CPU使用率 >80%  持续3次（每次1分钟）告警

内存使用率>80% 告警

任意磁盘分区使用率 > 80% 告警

任意磁盘设备IO 利用率 > 80% 告警

单网卡收/发流量  > 10M/s  （告警实例  这个可以自定义）

 告警方式

先写入本地日志 /var/log/日期_自定义名字.log

然后再通过邮件 命令发送给运维邮箱

注意两种 方式

1、手动 点菜单的方式调用查看资源使用情况

2、每分钟 执行一次







[toc]



# 前言：

作为linux 运维人员，在管理linux过程中，自动脚本虽是基本功，但监控脚本以及输出告警提醒工程师也是必不可少的一部分，这直接影响Linux 运行是否正常，接下来，我们一起走进Linux，看清它的“真面目”！

# 项目要求：

- 此项目共分为8个小组
- 其中7小组负责各搭建1个服务（本次服务分为七大模块，#1、CPU；#2、内存；#3、硬盘；#4、网络流量；#5、I/O；#6、DHCP；#7、DNS）
- 另外1个小组负责通过一个脚本来调用7个服务并且设置周期性任务实现每两分钟进行一次脚本监控
- 项目运行成功！！！

# 项目准备：

- 先把大的脚本框架搭起来，后面用
- system.sh（大脚本框架）

```shell
#!/bin/bash

function menu() 
{
read -p "欢迎使用本次服务。本次服务分为七大模块，#0、退出；#1、CPU；#2、内存；#3、硬盘；#4、网络流量；#5、I/O；#6、DHCP；#7、DNS；请输入您的需求(输入序号即可):" NUM
case $NUM  in
        1)
;;
        2)
        
;;
        3)
;;
        4)
;;
        5)
       
;;
        6)
        a="DHCP启动失败，请重新设置"
        source /root/dhcp.sh &>/dev/null
        if [ "dhcpd服务启动失败" ];then
                echo "$a"
        fi
;;
        7)
        source /root/dns.sh &>/dev/null
        if [ $? -eq 0 ];then
                echo "DNS服务启动成功"
        else
                echo "DNS服务启动失败存在错误"
        fi
;;
esac
        if [ $NUM -eq 0  ];then
        return
        fi

        menu #递归

}
menu
```



# 1.cpu

```shell
#!/bin/bash
x=$(top -b -n 1 | grep Cpu | awk -F ',' '{print $4}' | awk '{print $1*10}')
init(){
    if [ $x -le 200 ];then
         echo "当前CPU使用率超过80%请注意"
  else [ $x -ge 200 ]
         echo "当前CPU使用率未超过80%运行正常"
    fi

}

init
```

# 2.内存

```shell

#!/bin/bash
#name:mem.sh
#Desc:检测系统内存使用情况，超过80%就报警给管理员

total=$(free -m |awk 'NR==2{print $2}')
used=$(free -m |awk 'NR==2{print $3}')
syl=$[$used*100/$total]

if [ $syl -gt 80 ];then
        SUBJECT="ATTENTION: Memory Utilization is High on $(hostname) at $(date)"
        MESSAGE="/tmp/Mail.out"
        TO="1335811080@qq.com"
        echo "Memory Current Usage is: $syl%" >> $MESSAGE
        echo "" >> $MESSAGE
        echo "------------------------------------------------------------------" >> $MESSAGE
        echo "Top Memory Consuming Process Using top command" >> $MESSAGE
        echo "------------------------------------------------------------------" >> $MESSAGE
        echo "$(top -b -o +%MEM | head -n 20)" >> $MESSAGE
        echo "" >> $MESSAGE
        echo "------------------------------------------------------------------" >> $MESSAGE
        echo "Top Memory Consuming Process Using ps command" >> $MESSAGE
        echo "------------------------------------------------------------------" >> $MESSAGE
        echo "$(ps -eo pid,ppid,%mem,%Memory,cmd --sort=-%mem | head)" >> $MESSAGE
        mail -s "$SUBJECT" "$TO" < $MESSAGE
else
        echo "ATTENTION: Memory Utilization is ok on $(hostname) at $(date)" 
fi

```

# 3.硬盘

```shell
#!/bin/bash
dis=$(df -h|awk -F '[ %]+'  '/\/$/{print $5}')

check(){
    if [ $dis -ge 80 ];then
       echo "磁盘使用率超过80%，请注意！"
    else [ $dis -lt 80 ]
 echo "磁盘使用率未超过80%，运行正常"
    fi
}

check
```

# 4.网络流量

```shell
#!/bin/bash
read -p '请输入网络接口: ' INTER    #（使用read交互,请输入网络接口）
ifconfig $INTER &> /dev/null     #（查看网络接口信息，将标准输出、标准错误的内容保存到黑洞里面）
if [ $? -ne 0 ];then     #（当返回值不等于0时）
   echo "此网络接口不存在！"
   exit     #（退出）
fi

while true     #（使用while语句，反复执行）
do
   RX_before=`ifconfig $INTER | awk 'NR==5{print $5}'`     #（定义1秒前的下行流量，在ens33的第5行第5列）
   TX_before=`ifconfig $INTER | awk 'NR==7{print $5}'`     #（定义1秒前的上行流量，在ens33的第7行第5列）
   sleep 1     #（间隔时间1秒）
   RX_after=`ifconfig $INTER | awk 'NR==5{print $5}'`      #（定义1秒后的下行流量，在ens33的第5行第5列）
   TX_after=`ifconfig $INTER | awk 'NR==7{print $5}'`      #（定义1秒后的上行流量，在ens33的第7行第5列）
   RX=$((RX_after-RX_before))     #（定义下载速度：1秒后的下行流量”减去“1秒前的下行流量）
   TX=$((TX_after-TX_before))     #（定义上传速度：1秒后的上行流量”减去“1秒前的上行流量）
   clear     #（清理）
   echo -e "网络监测时间：`date +%Y年%m月%d日%T`"     #（echo网络监测时间）

   if [ $RX -le 1024 ];then     #（当下载速度小于1024B，执行以下命令）
      echo "下载速度为：$[$RX]B/s"
   elif [[ $RX -ge 1024 && $RX -lt 1048576 ]];then     #（当下载速度大于等于1KB且小于1M，执行以下命令）
      echo "下载速度为：$[$RX/1024]KB/s"
   elif [[ $RX -ge 1048576 && $RX -lt 20971520 ]];then     #（当下载速度大于等于1M且小于20M，执行以下命令）
      echo "下载速度为：$[$RX/1048576]MB/s"
   else     #（下载速度大于等于20M，并警告，执行以下命令）
      echo "警告：下载速度过大，存在恶意攻击！" >> /home/WL.txt
   fi
   if [ $TX -lt 1024 ];then     #（当上传速度小于1024B，执行以下命令）
      echo "上传速度为：$[$TX]B/s"
   elif [[ $TX -ge 1024 && $TX -lt 1048576 ]];then     #（当上传速度大于等于1KB且小于1M，执行以下命令）
      echo "上传速度为：$[$TX/1024]KB/s"
   elif [[ $RX -ge 1048576 && $RX -lt 10485760 ]];then     #（当下载速度大于等于1M且小于10M，执行以下命令）
      echo "上传速度为：$[$RX/1048576]MB/s"
   else     #（上传速度大于等于10M，并警告，执行以下命令）
      echo "警告：上传速度过大，存在恶意攻击！" >> /home/WL.txt
    fi
done
```

# 5.I/O

```shell
#!/bin/bash
echo "指定设备(/dev/sda)的统计信息"

# 每秒向设备发起的读请求次数

disk_sda_rs=`iostat -kx | grep sda| awk '{print $4}'`

echo "每秒向设备发起的读请求次数："$disk_sda_rs



# 每秒向设备发起的写请求次数

disk_sda_ws=`iostat -kx | grep sda| awk '{print $5}'`

echo "每秒向设备发起的写请求次数："$disk_sda_ws



# 向设备发起的I/O请求队列长度平均值

disk_sda_avgqu_sz=`iostat -kx | grep sda| awk '{print $9}'`

echo "向设备发起的I/O请求队列长度平均值"$disk_sda_avgqu_sz



# 每次向设备发起的I/O请求平均时间

disk_sda_await=`iostat -kx | grep sda| awk '{print $10}'`

echo "每次向设备发起的I/O请求平均时间："$disk_sda_await



# 向设备发起的I/O服务时间均值

disk_sda_svctm=`iostat -kx | grep sda| awk '{print $11}'`

echo "向设备发起的I/O服务时间均值："$disk_sda_svctm



# 向设备发起I/O请求的CPU时间百分占比

disk_sda_util=`iostat -kx | grep sda| awk '{print $12}'`

echo "向设备发起I/O请求的CPU时间百分占比："$disk_sda_util

```

# 6.dhcp

```shell
#!/bin/bash
#定义变量
#subnet=192.168.126.0

#netmask=255.255.255.0
#定义地址池
#range='192.168.126.100 192.168.126.200'
#网关
#gateway=192.168.126.2
#定义dhcp服务器ip


domain_name_servers=`ifconfig | awk '(NR<=10){print}' | awk '/broadcast/{print $2}'`


a=`ifconfig | awk '(NR<=10){print}' | awk '/broadcast/{print $2}'`


b=`ifconfig | awk '(NR<=10){print}' | awk '/broadcast/{print $2}' | awk -F "." '{print $4 }'`

echo $a > /var/1.txt
echo $a > /var/2.txt
echo $a > /var/3.txt
echo $a > /var/4.txt

sed -i "s|$b|2|g" /var/1.txt

sed -i "s|$b|0|g" /var/2.txt

sed -i "s|$b|100|g" /var/3.txt

sed -i "s|$b|200|g" /var/4.txt

gateway=`cat /var/1.txt`

subnet=`cat /var/2.txt`

c=`cat /var/3.txt`
d=`cat /var/4.txt`
netmask=255.255.255.0




range="$c $d"

domain_name=`hostname`
dhcpd_conf=/etc/dhcp/dhcpd.conf

#测试yum源
test_yum(){
yum clean all &> /dev/null
repo=`yum repolist 2> /dev/null | grep repolist | awk '{print $2}'`

if [ "$repo" != "0" ]
then
 [ -f $dhcpd_conf ] || yum install -y dhcp.x86_64   &> /dev/null

else
 echo 'yum源错误、请先配置yum源!'
 exit
fi
}

#yum install -y dhcp-devel.x86_64 

#备份原文件
backup () {
if [ -f $dhcpd_conf ]
then
 mv $dhcpd_conf $dhcpd_conf\.bak
fi
}

#新建配置文件
create_conf () {
cat > $dhcpd_conf << EOF
subnet $subnet netmask $netmask {
  range $range ;
  option domain-name-servers $domain_name_servers ;
  option domain-name "$domain_name";
  option routers $gateway ;
  default-lease-time 600;
  max-lease-time 7200;
}
EOF
}

#调用函数完成DHCP服务的搭建

test_yum
backup
create_conf
systemctl restart dhcpd &> /dev/null && echo 'dhcpd服务启动成功' || echo 'dhcpd服务启动失败'
systemctl enable dhcpd &> /dev/null && echo 'dhcpd服务加入开机自启成功' || echo 'dhcpd服务加入开机自启失败'
```

# 7.dns

```shell
#检测YUM源仓库是否挂载
df | grep "sr0"
if [ $? -eq 0 ]
        then
        yum -y install bind &> /dev/null
        #若已挂载则直接安装bind软件包
else
        mount /dev/cdrom /mnt &> /dev/null
        yum -y install bind &> /dev/null
        #若未挂载，则先挂载，再安装bind软件包"
fi

#关闭防火墙和强制访问控制安全系统
systemctl stop firewalld &> /dev/null
systemctl disable firewalld &> /dev/null
setenforce 0 &> /dev/null

#修改主配置文件
sed -i 's/127.0.0.1/any/' /etc/named.conf
sed -i 's/localhost/any/' /etc/named.conf

#修改区域配置文件，添加正向区域配置
sed -i 's/localhost.localdomain/benet.com/' /etc/named.rfc1912.zones
sed -i 's/named.localhost/benet.com.zone/' /etc/named.rfc1912.zones
sed -i '18,100d' /etc/named.rfc1912.zones

#配置正向区域数据文件
cd /var/named
cp -p named.localhost benet.com.zone
sed -i 's/@/benet.com./g' /var/named/benet.com.zone
sed -i 's/rname.invalid./admin.benet.com./' /var/named/benet.com.zone
sed -i 's/127.0.0.1/192.168.91.103/' /var/named/benet.com.zone
sed -i '9a www IN A  192.168.126.15' /var/named/benet.com.zone
sed -i '/AAAA/d' /var/named/benet.com.zone
sed -i '/::1/d' /var/named/benet.com.zone

#启动服务
systemctl start named

#在客户端的域名解析配置文件中添加DNS服务器地址
sed -i '1a nameserver 192.168.91.103' /etc/resolv.conf

read -p "DNS正向解析已配置完毕，请输入域名：" p
host $p
```

# 8.全部服务(大脚本)

```shell
#!/bin/bash

function menu() 
{
read -p "欢迎使用本次服务。本次服务分为七大模块，#0、退出；#1、CPU；#2、内存；#3、硬盘；#4、网络流量；#5、I/O；#6、DHCP；#7、DNS；请输入您的需求(输入序号即可):" NUM
case $NUM  in
        1)

x=$(top -b -n 1 | grep Cpu | awk -F ',' '{print $4}' | awk '{print $1*10}')
init(){
    if [ $x -le 200 ];then
         echo "当前CPU使用率超过80%请注意"
  else [ $x -ge 200 ]
         echo "当前CPU使用率未超过80%运行正常"
    fi

}

init
;;
        2)
        
total=$(free -m |awk 'NR==2{print $2}')
used=$(free -m |awk 'NR==2{print $3}')
syl=$[$used*100/$total]

if [ $syl -gt 80 ];then
        SUBJECT="ATTENTION: Memory Utilization is High on $(hostname) at $(date)"
        MESSAGE="/tmp/Mail.out"
        TO="1335811080@qq.com"
        echo "Memory Current Usage is: $syl%" >> $MESSAGE
        echo "" >> $MESSAGE
        echo "------------------------------------------------------------------" >> $MESSAGE
        echo "Top Memory Consuming Process Using top command" >> $MESSAGE
        echo "------------------------------------------------------------------" >> $MESSAGE
        echo "$(top -b -o +%MEM | head -n 20)" >> $MESSAGE
        echo "" >> $MESSAGE
        echo "------------------------------------------------------------------" >> $MESSAGE
        echo "Top Memory Consuming Process Using ps command" >> $MESSAGE
        echo "------------------------------------------------------------------" >> $MESSAGE
        echo "$(ps -eo pid,ppid,%mem,%Memory,cmd --sort=-%mem | head)" >> $MESSAGE
        mail -s "$SUBJECT" "$TO" < $MESSAGE
else
        echo "ATTENTION: Memory Utilization is ok on $(hostname) at $(date)" 
fi
;;
        3)

dis=$(df -h|awk -F '[ %]+'  '/\/$/{print $5}')

check(){
    if [ $dis -ge 80 ];then
       echo "磁盘使用率超过80%，请注意！"
    else [ $dis -lt 80 ]
 echo "磁盘使用率未超过80%，运行正常。"
    fi
}
check
;;
        4)

read -p '请输入网络接口: ' INTER    #（使用read交互,请输入网络接口）
ifconfig $INTER &> /dev/null     #（查看网络接口信息，将标准输出、标准错误的内容保存到黑洞里面）
if [ $? -ne 0 ];then     #（当返回值不等于0时）
   echo "此网络接口不存在！"
   exit     #（退出）
fi

while true     #（使用while语句，反复执行）
do
   RX_before=`ifconfig $INTER | awk 'NR==5{print $5}'`     #（定义1秒前的下行流量，在ens33的第5行第5列）
   TX_before=`ifconfig $INTER | awk 'NR==7{print $5}'`     #（定义1秒前的上行流量，在ens33的第7行第5列）
   sleep 1     #（间隔时间1秒）
   RX_after=`ifconfig $INTER | awk 'NR==5{print $5}'`      #（定义1秒后的下行流量，在ens33的第5行第5列）
   TX_after=`ifconfig $INTER | awk 'NR==7{print $5}'`      #（定义1秒后的上行流量，在ens33的第7行第5列）
   RX=$((RX_after-RX_before))     #（定义下载速度：1秒后的下行流量”减去“1秒前的下行流量）
   TX=$((TX_after-TX_before))     #（定义上传速度：1秒后的上行流量”减去“1秒前的上行流量）
   clear     #（清理）
   echo -e "网络监测时间：`date +%Y年%m月%d日%T`"     #（echo网络监测时间）

   if [ $RX -le 1024 ];then     #（当下载速度小于1024B，执行以下命令）
      echo "下载速度为：$[$RX]B/s"
   elif [[ $RX -ge 1024 && $RX -lt 1048576 ]];then     #（当下载速度大于等于1KB且小于1M，执行以下命令）
      echo "下载速度为：$[$RX/1024]KB/s"
   elif [[ $RX -ge 1048576 && $RX -lt 20971520 ]];then     #（当下载速度大于等于1M且小于20M，执行以下命令）
      echo "下载速度为：$[$RX/1048576]MB/s"
   else     #（下载速度大于等于20M，并警告，执行以下命令）
      echo "警告：下载速度过大，存在恶意攻击！" >> /home/WL.txt
   fi
   if [ $TX -lt 1024 ];then     #（当上传速度小于1024B，执行以下命令）
      echo "上传速度为：$[$TX]B/s"
   elif [[ $TX -ge 1024 && $TX -lt 1048576 ]];then     #（当上传速度大于等于1KB且小于1M，执行以下命令）
      echo "上传速度为：$[$TX/1024]KB/s"
   elif [[ $RX -ge 1048576 && $RX -lt 10485760 ]];then     #（当下载速度大于等于1M且小于10M，执行以下命令）
      echo "上传速度为：$[$RX/1048576]MB/s"
   else     #（上传速度大于等于10M，并警告，执行以下命令）
      echo "警告：上传速度过大，存在恶意攻击！" >> /home/WL.txt
    fi
done   
;;
        5)

echo "指定设备(/dev/sda)的统计信息"

# 每秒向设备发起的读请求次数

disk_sda_rs=`iostat -kx | grep sda| awk '{print $4}'`

echo "每秒向设备发起的读请求次数："$disk_sda_rs



# 每秒向设备发起的写请求次数

disk_sda_ws=`iostat -kx | grep sda| awk '{print $5}'`

echo "每秒向设备发起的写请求次数："$disk_sda_ws



# 向设备发起的I/O请求队列长度平均值

disk_sda_avgqu_sz=`iostat -kx | grep sda| awk '{print $9}'`

echo "向设备发起的I/O请求队列长度平均值"$disk_sda_avgqu_sz



# 每次向设备发起的I/O请求平均时间

disk_sda_await=`iostat -kx | grep sda| awk '{print $10}'`

echo "每次向设备发起的I/O请求平均时间："$disk_sda_await



# 向设备发起的I/O服务时间均值

disk_sda_svctm=`iostat -kx | grep sda| awk '{print $11}'`

echo "向设备发起的I/O服务时间均值："$disk_sda_svctm



# 向设备发起I/O请求的CPU时间百分占比

disk_sda_util=`iostat -kx | grep sda| awk '{print $12}'`

echo "向设备发起I/O请求的CPU时间百分占比："$disk_sda_util

;;
        6)

#定义变量
#subnet=192.168.126.0

#netmask=255.255.255.0
#定义地址池
#range='192.168.126.100 192.168.126.200'
#网关
#gateway=192.168.126.2
#定义dhcp服务器ip


domain_name_servers=`ifconfig | awk '(NR<=10){print}' | awk '/broadcast/{print $2}'`


a=`ifconfig | awk '(NR<=10){print}' | awk '/broadcast/{print $2}'`


b=`ifconfig | awk '(NR<=10){print}' | awk '/broadcast/{print $2}' | awk -F "." '{print $4 }'`

echo $a > /var/1.txt
echo $a > /var/2.txt
echo $a > /var/3.txt
echo $a > /var/4.txt

sed -i "s|$b|2|g" /var/1.txt

sed -i "s|$b|0|g" /var/2.txt

sed -i "s|$b|100|g" /var/3.txt

sed -i "s|$b|200|g" /var/4.txt

gateway=`cat /var/1.txt`

subnet=`cat /var/2.txt`

c=`cat /var/3.txt`
d=`cat /var/4.txt`
netmask=255.255.255.0




range="$c $d"

domain_name=`hostname`
dhcpd_conf=/etc/dhcp/dhcpd.conf

#测试yum源
test_yum(){
yum clean all &> /dev/null
repo=`yum repolist 2> /dev/null | grep repolist | awk '{print $2}'`

if [ "$repo" != "0" ]
then
 [ -f $dhcpd_conf ] || yum install -y dhcp.x86_64   &> /dev/null

else
 echo 'yum源错误、请先配置yum源!'
 exit
fi
}

#yum install -y dhcp-devel.x86_64 

#备份原文件
backup () {
if [ -f $dhcpd_conf ]
then
 mv $dhcpd_conf $dhcpd_conf\.bak
fi
}

#新建配置文件
create_conf () {
cat > $dhcpd_conf << EOF
subnet $subnet netmask $netmask {
  range $range ;
  option domain-name-servers $domain_name_servers ;
  option domain-name "$domain_name";
  option routers $gateway ;
  default-lease-time 600;
  max-lease-time 7200;
}
EOF
}

#调用函数完成DHCP服务的搭建

test_yum
backup
create_conf
systemctl restart dhcpd &> /dev/null && echo 'dhcpd服务启动成功' || echo 'dhcpd服务启动失败'
systemctl enable dhcpd &> /dev/null && echo 'dhcpd服务加入开机自启成功' || echo 'dhcpd服务加入开机自启失败'

        a="DHCP启动失败，请重新设置"
        source /root/dhcp.sh &>/dev/null
        if [ "dhcpd服务启动失败" ];then
                echo "$a"
        fi
;;

        7)

#检测YUM源仓库是否挂载
df | grep "sr0"
if [ $? -eq 0 ]
        then
        yum -y install bind &> /dev/null
        #若已挂载则直接安装bind软件包
else
        mount /dev/cdrom /mnt &> /dev/null
        yum -y install bind &> /dev/null
        #若未挂载，则先挂载，再安装bind软件包"
fi

#关闭防火墙和强制访问控制安全系统
systemctl stop firewalld &> /dev/null
systemctl disable firewalld &> /dev/null
setenforce 0 &> /dev/null

#修改主配置文件
sed -i 's/127.0.0.1/any/' /etc/named.conf
sed -i 's/localhost/any/' /etc/named.conf

#修改区域配置文件，添加正向区域配置
sed -i 's/localhost.localdomain/benet.com/' /etc/named.rfc1912.zones
sed -i 's/named.localhost/benet.com.zone/' /etc/named.rfc1912.zones
sed -i '18,100d' /etc/named.rfc1912.zones

#配置正向区域数据文件
cd /var/named
cp -p named.localhost benet.com.zone
sed -i 's/@/benet.com./g' /var/named/benet.com.zone
sed -i 's/rname.invalid./admin.benet.com./' /var/named/benet.com.zone
sed -i 's/127.0.0.1/192.168.126.15/' /var/named/benet.com.zone
sed -i '9a www IN A  192.168.126.15' /var/named/benet.com.zone
sed -i '/AAAA/d' /var/named/benet.com.zone
sed -i '/::1/d' /var/named/benet.com.zone

#启动服务
systemctl start named

#在客户端的域名解析配置文件中添加DNS服务器地址
sed -i '1a nameserver 192.168.126.15' /etc/resolv.conf

read -p "DNS正向解析已配置完毕，请输入域名：" p
host $p
        source /root/system.sh
        if [ $? -eq 0 ];then
                echo "DNS服务启动成功"
        else
                echo "DNS服务启动失败存在错误"
        fi
;;
esac
        if [ $NUM -eq 0  ];then
        return
        fi

        menu #递归
}
menu

```

# 9.crontab设置

```shell
chmod 777 system.sh 				    #修改脚本的权限
crontab -e							   #设置周期性任务
*/2 * * * * sh /root/system.sh            #每两分钟调用一次脚本

service crond start 				     #设置完了之后不要忘了，启动定时服务（大多数忘了这里）
service crond stop						#关闭定时服务
service crond restart 					#重启服务
service crond reload 					#重新载入配置

tail -f /var/log/cron					#查看crontab运行状态

#注意：新创建的 cron 任务，不会马上执行，至少要过 2 分钟后才可以，当然你可以重启 cron 来马上执行
```

# 10.验证

```shell
[root@localhost ~]# . system.sh 

欢迎使用本次服务。本次服务分为七大模块，#0、退出；#1、CPU；#2、内存；#3、硬盘；#4、网络流量；#5、I/O；#6、DHCP；#7、DNS；请输入您的需求(输入序号即可):1
当前CPU使用率未超过80%运行正常

欢迎使用本次服务。本次服务分为七大模块，#0、退出；#1、CPU；#2、内存；#3、硬盘；#4、网络流量；#5、I/O；#6、DHCP；#7、DNS；请输入您的需求(输入序号即可):2
ATTENTION: Memory Utilization is ok on localhost.localdomain at 2021年 09月 21日 星期二 21:03:54 CST

欢迎使用本次服务。本次服务分为七大模块，#0、退出；#1、CPU；#2、内存；#3、硬盘；#4、网络流量；#5、I/O；#6、DHCP；#7、DNS；请输入您的需求(输入序号即可):3
磁盘使用率未超过80%，运行正常

欢迎使用本次服务。本次服务分为七大模块，#0、退出；#1、CPU；#2、内存；#3、硬盘；#4、网络流量；#5、I/O；#6、DHCP；#7、DNS；请输入您的需求(输入序号即可):4
请输入网络接口: ens33
网络监测时间：2021年09月21日21:05:13
下载速度为：964B/s
上传速度为：2KB/s


欢迎使用本次服务。本次服务分为七大模块，#0、退出；#1、CPU；#2、内存；#3、硬盘；#4、网络流量；#5、I/O；#6、DHCP；#7、DNS；请输入您的需求(输入序号即可):5
指定设备(/dev/sda)的统计信息
每秒向设备发起的读请求次数：4.07
每秒向设备发起的写请求次数：14.60
向设备发起的I/O请求队列长度平均值0.04
每次向设备发起的I/O请求平均时间：2.27
向设备发起的I/O服务时间均值：0.39
向设备发起I/O请求的CPU时间百分占比：2.80

欢迎使用本次服务。本次服务分为七大模块，#0、退出；#1、CPU；#2、内存；#3、硬盘；#4、网络流量；#5、I/O；#6、DHCP；#7、DNS；请输入您的需求(输入序号即可):6
dhcpd服务启动成功
dhcpd服务加入开机自启成功

欢迎使用本次服务。本次服务分为七大模块，#0、退出；#1、CPU；#2、内存；#3、硬盘；#4、网络流量；#5、I/O；#6、DHCP；#7、DNS；请输入您的需求(输入序号即可):7
/dev/sr0        4600876 4600876        0  100% /run/media/root/CentOS 7 x86_64
DNS正向解析已配置完毕，请输入域名：www.benet.com
www.benet.com has address 192.168.126.15
---------------------------------
#周期性任务验证——每两分钟执行一次脚本
[root@localhost ~]# tail -f /var/log/cron
Sep 21 20:58:01 localhost CROND[20633]: (root) CMD (sh /root/system.sh)
Sep 21 21:00:01 localhost CROND[23598]: (root) CMD (/usr/lib64/sa/sa1 1 1)
Sep 21 21:00:01 localhost CROND[23599]: (root) CMD (sh /root/system.sh)
Sep 21 21:01:01 localhost CROND[25052]: (root) CMD (run-parts /etc/cron.hourly)
Sep 21 21:01:01 localhost run-parts(/etc/cron.hourly)[25052]: starting 0anacron
Sep 21 21:01:01 localhost run-parts(/etc/cron.hourly)[25061]: finished 0anacron
Sep 21 21:02:01 localhost CROND[26342]: (root) CMD (sh /root/system.sh)
Sep 21 21:04:01 localhost CROND[28998]: (root) CMD (sh /root/system.sh)
Sep 21 21:06:01 localhost CROND[32479]: (root) CMD (sh /root/system.sh)
Sep 21 21:08:01 localhost CROND[37047]: (root) CMD (sh /root/system.sh)


```







